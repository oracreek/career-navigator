name = "career-navigator-api"
main = "src/index.js"
compatibility_date = "2024-12-01"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "career-navigator-db"
database_id = "" # Will be filled after creating the database

# R2 Bucket binding (for future PDF storage)
# [[r2_buckets]]
# binding = "FILES"
# bucket_name = "career-navigator-files"

# Environment variables
[vars]
# Add any non-secret environment variables here

# Secrets (set via: wrangler secret put ANTHROPIC_API_KEY)
# ANTHROPIC_API_KEY = "your-key-here" (use wrangler secret put)
